const Login = (props) => {
    return (
        <div>
            <div className="text-3xl font-bold underline">Login</div>
            <p>Username: {props.username}</p>
        </div>
    )
}

export default Login;